# 领域资料

智慧交通、电网巡检和算力项目在当地保留原始数据，只交换需求基线、环境摘要、系统版本和签名验收结果。

外部主体使用不含真实身份信息的引用编号。交换时间采用带偏移量的 ISO 8601 字符串，原始材料只保存受控引用或 `sha256` 摘要。`contracts/entities.json` 中的字段名称属于稳定接口约定，运行时数据文件位置由 `DATABASE_PATH` 决定。
